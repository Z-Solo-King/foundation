name: deploy-public-worker-manual

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Public Worker deployment is performed by the known-good public-tests workflow on main pushes."
