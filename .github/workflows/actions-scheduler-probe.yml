name: actions-scheduler-probe

on:
  push:
    branches: [actions-scheduler-probe]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo ACTIONS_SCHEDULER_PROBE_OK
