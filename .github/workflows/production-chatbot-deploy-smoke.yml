name: production-chatbot-deploy-smoke

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  deploy-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Smoke workflow probe
        run: echo PRODUCTION_SMOKE_WORKFLOW_PARSED
