name: chatbot-live-infrastructure-smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Canonical live chatbot Cloudflare/B2 verification runs after public-tests."
