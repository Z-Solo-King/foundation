"""Repository-owned benchmark and research validation modules.

This explicit package marker prevents an unrelated installed ``benchmark``
namespace from shadowing the project's benchmark modules during tests and CI.
"""
