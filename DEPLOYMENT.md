# Deployment

This repository contains the public contract/Worker boundary. Production control-plane deployment details are intentionally kept outside the public repository.

Use the repository's checked-in deployment configuration only with credentials and service bindings supplied through the deployment environment.

Do not commit API tokens, authentication secrets, private endpoints, internal service names, database identifiers, or private operational configuration.
