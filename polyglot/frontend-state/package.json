{
  "name": "foundation-frontend-state-shadow",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build-browser": "tsc -p tsconfig.browser.json",
    "test": "npm run build-browser && node --experimental-strip-types --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "22.15.0",
    "typescript": "5.8.3"
  }
}
